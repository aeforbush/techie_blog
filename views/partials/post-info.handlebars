<div class="card">
    <div class="card-header">
        <h4 class="link" href="/post/{{id}}">{{title}}</h4>
        <p>Posted by {{user.email}} on {{format_date created_at}}</p>
    </div>
    <p class="card-body">{{content}}</p>
</div>